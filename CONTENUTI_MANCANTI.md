# Item con file a 0 byte nella consegna "CD TS OK" (contenuto mancante)

Questi item sono stati copiati in `contents/tavolo/` ma il loro file immagine/PDF è di **0 byte** (contenuto non trasferito nella consegna). L'ingest li ha **saltati**: non compaiono nell'app.
Riconsegnare i file originali, reincollarli nelle stesse cartelle, poi rilanciare `npm run ingest-contents`.

---

**TOTALE item con contenuto mancante: 0**
